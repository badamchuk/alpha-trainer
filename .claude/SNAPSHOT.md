# SNAPSHOT — AlphaTrainer

_Оновлюється в кінці кожної сесії через `/finish`_

## Останнє оновлення
2026-09-18

## Що зроблено
- Всі основні екрани: головна, харчування, прогрес, цілі, журнал, AI-тренер
- AI тренер: streaming chat, пам'ять між сесіями, генерація плану тренувань; бачить харчування за 3 дні
- AI нутріціолог: окремий чат у вкладці харчування (✨ кнопка в хедері); бачить 7-денну КБЖВ + тренування
- Харчування: навігація по датах (← →), редагування, клітковина, бібліотека страв
- Харчування: "Повторити з правками" (copy-outline) — копіює прийом для редагування як новий
- Харчування: тижневий графік калорій (7 барів, клікабельні, червоний при перевищенні цілі)
- Журнал: "Повторити з правками" — відкриває log.tsx з передзаповненими вправами/типом
- log.tsx: підтримка `repeatId` URL param, синій банер-підказка при повторенні
- Профіль: стать у формулі Mifflin-St Jeor; fitnessLevel впливає на TDEE (+2.5%/+5%)
- Резервне копіювання: всі ключі включно з daily_advice та nutritionist_chat_history
- KeyboardAvoidingView у всіх модалах з TextInput
- **НОВE (2026-04-19):**
  - Штрихкод-сканер: `services/foodAI.ts` + `components/BarcodeScannerModal.tsx`
    - Кнопка "barcode-outline" поряд з "Додати прийом їжі" в nutrition.tsx
    - expo-camera CameraView, Open Food Facts API (безкоштовно, без ключа)
    - Вибір кількості грамів після сканування, автопідрахунок
  - Фото-логування їжі: кнопка "camera-outline" в nutrition.tsx
    - expo-image-picker (камера або галерея) → base64 → Gemini Vision
    - Використовує gemini-1.5-flash, потрібен Gemini API ключ
  - Адаптивний TDEE: `computeAdaptiveTDEE()` в nutrition.ts
    - Аналіз 28 днів ваги + калорій, MacroFactor-алгоритм
    - Відображається в progress.tsx з рівнем впевненості + порадою
  - Кореляція їжа→результати: `computeFoodCorrelation()` в nutrition.ts
    - Порівнює оцінки/тривалість тренувань після "добрих" vs "поганих" днів харчування
    - Показується в progress.tsx якщо є достатньо даних (≥7 днів харчування, ≥5 тренувань)
  - app.json: додано expo-camera + expo-image-picker plugins з дозволами
- **НОВЕ (2026-08-13):**
  - Політні підходи (`setsDetail`): різна вага/повтори в межах вправи;
    зведені `sets/reps/weight` = кількість + найважчий підхід
  - Схеми таймера (`services/timerPrefs.ts`) + переписаний RestTimer
  - Система досягнень: 14 ачівок, показ у progress.tsx
  - **Редагування вправ**: тап по рядку або олівець відкриває вправу у формі.
    Працює і при логуванні, і в збереженому тренуванні
  - **Суперсети заднім числом**: "Об'єднати в суперсет" → чекбокси → "Об'єднати";
    "Розгрупувати" на заголовку групи. Логіка — `services/supersets.ts`
  - **Переміщення вправ** стрілками ↑↓ на обох екранах
  - Кнопку редагування в деталях підписано ("Змінити") — сірий олівець серед
    трьох іконок ніхто не знаходив
  - Надійність сховища: `readJSON()` не роняє додаток на пошкоджених даних,
    `withLock()` не дає паралельним записам перетирати один одного
  - Продуктивність: екран прогресу читає історію 1 раз замість 3;
    ачівки рахуються за один прохід замість чотирьох
  - Версія + хеш коміту + час збірки в Профіль → AI-моделі
  - Тести: jest + 57 тестів (`npm test`), покрито supersets/storage/analytics
  - Вирівняно 15 пакетів під Expo 55, expo-doctor 19/19
- **НОВЕ (2026-09-16):**
  - **Суперсети в збереженому**: кнопка «Суперсет» просто в перегляді тренування
    (відмітив → «Об'єднати» → збережено одразу, там же «Розгрупувати»).
    Кнопки вибору — на `components/SupersetBar.tsx`, закріпленій унизу, і при
    логуванні, і в «Змінити». Причина скарги: у тренуваннях на 10–22 вправи
    «Об'єднати» над списком виїжджала за екран. «Суперсет» з однієї вправи
    прибирається при збереженні
  - **Калорії по вправах** (`services/calories.ts`): (MET−1)×вага + BMR/24
    (Mifflin-St Jeor), вага на дату з журналу ваги; темп/вати для кардіо;
    значення на підхід; інтервальні (скакалка, вибухові) — робота + відпочинок.
    Показ: деталі тренування, жива оцінка при логуванні, «Прогрес»
  - **Шаблони**: редагування (`log.tsx?templateId=…` — та сама форма),
    видалення, «Оновити «назва»» для шаблону, з якого почали
  - **Оновлення через GitHub**: `services/updates.ts` (перевірка `releases/latest`
    на старті й при поверненні, не частіше ніж раз на 6 год; кнопка в
    Профіль → AI-моделі), `npm run release` (`scripts/release.js`)
  - Release-збірка перевірена на емуляторі на копії реальних даних і
    встановлена на Pixel поверх debug — дані збереглись
  - Тести: 101 (додано calories, updates)
- **НОВЕ (2026-09-18) — бібліотека вправ, заміни, конструктор, ілюстрації:**
  - **Бібліотека** `services/library/` — 181 вправа: патерн руху, сімейство,
    рівень 1–3, намір (сила/маса/потужність/кондиція/ізометрія/мобільність),
    м'язи, обладнання, навантаження на суглоби 1–3, зв'язки «простіша за»,
    підказки техніки, `metKind` для калорій. Індекси ліниві
  - **Розпізнавання назв** `services/exerciseMatch.ts` + екран
    «Профіль → Тренування → Розпізнавання вправ»: 89% записів реальної історії
    прив'язується самі, 95% після одного зв'язку вручну. Нечіткий збіг — лише
    підказка, ніколи не застосовується автоматично. Записи НЕ переписуються
  - **Аналітика за id**: резолвер необов'язковим останнім аргументом у
    `getExerciseProgress`, `getExerciseList`, `getPersonalRecords` (analytics і
    storage), `getOverloadSuggestion`, `getMuscleGroupBalance`,
    `getVolumeLandmarks`, `getStrengthScore`, `estimateWorkoutCalories`.
    Реальна історія: 152 рядки прогресу → 77; метокони більше не рахуються
    в гіпертрофічний об'єм; калорії 13 689 → 12 873 (−6%, див. docs/reports/after.md)
  - **Заміни** `services/substitutions.ts` + `components/SubstitutionSheet.tsx`:
    лише споріднений рух, фільтр обладнання й бережених зон, пояснення до
    кожного варіанта, перенесення схеми підходів, конверсія кардіо (2-хв якір)
  - **Конструктор** `services/builder.ts` + `app/workout/builder.tsx`:
    Фулбоді / Кросфіт × 30/45/60 хв × сила/маса/витривалість, детермінований
    (seed + attempt), підгонка під час ±15%, чернетка переживає вихід з екрана
  - **Ілюстрації**: 148 вправ × 3 кадри = 3.88 МБ WebP, CC BY-SA 4.0
    (Workout Guide / Everkinetic), екран «Джерела ілюстрацій», збирач
    `scripts/dev/build-exercise-images.py`
  - **Картка вправи** `app/exercises/[id].tsx`: анімація, техніка, «Те саме, але…»,
    простіше / варіації / складніше, твоя історія по вправі
  - Профіль: «Що берегти» (постійні зони), `equipmentIds` виводяться зі старих рядків
  - Тести: 246; критерії приймання ТЗ закріплені в `__tests__/acceptance.test.ts`
  - Документи: `docs/02-tz.md` (ТЗ), `docs/04-plan.md` (план), `docs/reports/`
    (до/після), `FEEDBACK.md` (для відгуків користувача)

- **НОВЕ (18.09, друга частина) — AI на одному рушії з додатком:**
  - `services/aiContext.ts` — тренер отримує перелік доступних вправ (обладнання,
    зони «берегти», рівень), канонічну історію через резолвер і сьогоднішню дату;
    відповідає з позначками `[id]`, під відповіддю — вправи з картинками й
    кнопка «Почати тренування з цих вправ». Перевірено живим запитом до Gemini:
    тренер сам врахував «берегти плече»
  - **Виправлено поламаний чат**: Gemini SDK падав у Hermes на Web Streams
    («pipeThrough of undefined») — тепер при такому збої відповідь береться цілком
  - Моделі оновлено: `gemini-2.5-flash → 2.5-pro → 2.0-flash → 2.0-flash-lite`
    (покоління 1.5 й експериментальні прибрано; є тест, щоб не повернулись)
  - План від AI: id з позначок тренера, нумеровані списки, чистка назв, відсів
    рядків підходів і коментарів; у дні плану — мініатюри й «Почати тренування
    за планом». На реальній відповіді: 12 вправ з 13 прив'язані (було 0)
  - Автокопія раз на тиждень у теку документів (три останні), показ у Профілі
  - `components/RichText.tsx` — розмітка у відповідях AI більше не показується
    зірочками
  - Конструктор доступний щодня, у т.ч. з вкладки «Тренер» без ключа
  - Рівень підготовки більше не ховає знайомі вправи (станова у середнього рівня)
  - Файловий API переведено з застарілого `writeAsStringAsync` на `File`
  - Тести: 318

## В процесі / наступне
- **Нічого з роботи 18.09 не закомічено** — чекає на явну команду
- **Ключ Groq не приймається** («Access denied» на живому запиті), а додаток
  обирає Groq, коли він заданий. Або оновити ключ, або прибрати його з профілю,
  щоб працював Gemini
- Живі відгуки: `FEEDBACK.md` заповнює користувач, далі розбираємо
- Не зроблено з ТЗ (SHOULD): вправи плану через розпізнавання й «Замінити» в дні
  плану (F8.1–F8.2); каталог вправ окремим екраном (зараз — модальний вибір)
- Видалення шаблону кошиком не перевірене наживо: емулятор завис через
  завантаженість Mac. Код простий, але варто глянути
- **Спільна форма вправи**: `log.tsx` і `workout/[id].tsx` містять по ~200 рядків
  майже однакового JSX форми. Свідомо не рефакторив — тести покривають сервіси,
  а не верстку, тож сітки безпеки для цього немає. Варте окремої сесії.
  Тепер `log.tsx` ще й має режим шаблону — аргумент на користь рефакторингу
- Тести на UI-компоненти (зараз лише сервіси)

## Відомі проблеми
- `newArchEnabled: false` в expo-build-properties більше не діє: RN 0.83
  стартує з `"fabric":true` і BridgelessReactContext незалежно від прапорця
- Дистанцію бігу можна вказати і на рівні тренування, і у вправі. Обидва
  варіанти тепер рахуються, але користувачу ніде не сказано, що є два способи
- Gradle намертво зависає, якщо Mac засне посеред збірки (не відповідає навіть
  на `--stop`) — збирати через `caffeinate -i`, release-скрипт так і робить
- Емулятор на завантаженому Mac (ігри, 16 ГБ RAM) ловить ANR самої системи —
  автотести UI на ньому нестабільні; uiautomator dump теж падає по таймауту
- Під час автоматичної прокрутки в режимі вибору суперсету один раз
  відмітилась зайва вправа; відтворити не вдалось (прокрутка не відмічає)
- Екран розпізнавання на 150 назвах думає ~0.6 с у дебаг-збірці (було 3.9 с).
  У release-збірці швидше, але якщо назв стане під тисячу — вантажити порціями
- `.expo/types/router.d.ts` генерується dev-сервером; нові маршрути я дописував
  туди руками, щоб typecheck проходив без запуску Metro

## Важливі рішення
- Дати: завжди `getLocalDateString()`, ніколи UTC математика
- selectedDate передається явно у всі loadData() виклики
- Groq пріоритетніший за Gemini (швидший, безкоштовний)
- Всі нові AsyncStorage ключі → backup.ts ALL_KEYS
- Повторення тренування: копіює exercises/type/notes, але не duration/rating/date
- Фото-логування: потребує gemini-1.5-flash (multimodal), Groq не підтримує Vision
- Адаптивний TDEE: потрібно ≥4 записів ваги і ≥7 днів харчування для розрахунку
- Open Food Facts: User-Agent обов'язковий (інакше 403)
- Пошкоджені дані у сховищі НЕ видаляються, а відкладаються під ключ
  `:corrupt:<час>` — інакше наступний запис поверх порожнього масиву стер би
  всю історію остаточно
- Суперсет — це блок: при групуванні вправи фізично стають сусідніми, при
  переміщенні група їде цілком. Інакше порядок у масиві розходився б з тим,
  що показано на екрані
- Дві функції `getPersonalRecords` (storage і analytics) — це навмисно:
  перша рахує максимальну вагу, друга розрахунковий 1RM. Обидві бачать setsDetail
- `Constants.expoConfig.extra` береться з НАТИВНОЇ збірки, не з Metro. Тому
  хеш коміту показує код у APK; JS поверх нього може бути свіжішим
- JS-овий `\b` не працює після кирилиці — короткі слова матчити з явними
  межами. Так само "розгинання ніг" містить у собі "згинання ніг", тому
  порядок перевірок у classifyExercise має значення
- Шапки екранів: `paddingTop` через `insets.top`, ніколи числом. Виняток —
  вміст усередині `Modal`, де insets можуть дати 0 (там `Math.max`)
- Якщо правки не доїжджають на телефон — перевірити `adb reverse tcp:8081 tcp:8081`.
  Без тунелю додаток мовчки крутить закешований бандл, без жодної помилки
- Час, дистанція й ккал у вправі — на підхід (так пише користувач:
  «Гребля 6 підх. 16 ккал» = 96). Встановлено за реальними даними, не вгадано
- Оновлення: локальна збірка + GitHub Release, а не CI. APK має бути підписаний
  тим самим `android/app/debug.keystore`, що й установлений додаток, — інакше
  лише перевстановлення з втратою локальних даних. versionCode = semver
  (1.2.3 → 1002003), версія живе тільки в package.json
- Калорії: силові — сесійний MET на весь час разом з відпочинком; інтервальні —
  робочий MET на робочі секунди + 2 MET на відпочинок; тривалість тренування
  розподіляється між вправами. Вписані ккал мають пріоритет, ккал з годинника
  розкладаються по вправах пропорційно
